# Secrets and local environment - never commit
.env
*.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# OS
.DS_Store

# Claude and AI tooling - keep out of the repo
.claude/
CLAUDE.md

# Large intermediate audio - we keep the mp3 deliverable and the transcript, not the raw wav
results/recordings/*.wav
